{
  "name": "welink",
  "private": true,
  "description": "We Link — static site + Vercel Functions (no build step for the site itself; this package.json exists only to supply dependencies for /api functions).",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "docxtemplater": "^3.50.0",
    "pizzip": "^3.1.7",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  }
}
